// derived from requirejs/example-libglobal
(function (root, factory) {
    if (typeof define === 'function' && define.amd) {
        // AMD.
        define(factory);
    } else {
        // Browser globals
        root.splotch = factory();
    }
}(this, function () {
